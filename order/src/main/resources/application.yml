spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: order-service

server:
  port: 11000

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/